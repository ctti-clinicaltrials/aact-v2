class Admin::StyleGuideController < Admin::BaseController
  # Internal design-token reference. Admin-gated via Admin::BaseController and
  # intentionally not linked in the nav — reachable by URL only.
  def show
  end
end
